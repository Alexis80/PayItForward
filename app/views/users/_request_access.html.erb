<!--modified from railstutorial.org on 4/22/18-->

<%= form_for(current_user.active_relationships.build) do |f| %>
  <div><%= hidden_field_tag :receiver_id, current_user.id %></div>
  <div><%= hidden_field_tag :donor_id, @user.id %></div>
  <%= f.submit "Give Access", class: "btn btn-primary" %>
  
<% end %>

